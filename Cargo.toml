[package]
name = "embeddenator-retrieval"
version = "0.1.0"
edition = "2021"
authors = ["Tyler Zervas <tz-dev@vectorweight.com>"]
description = "Signature-based retrieval and resonator for holographic engrams"
license = "MIT"
repository = "https://github.com/tzervas/embeddenator-retrieval"
keywords = ["embeddenator", "vsa", "holographic", "sparse-ternary"]
categories = ["algorithms", "data-structures", "memory-management"]

[features]
default = []
simd = []
block-sparse = []

[dependencies]
# Component dependencies will be minimal
# Add specific deps as needed per component

[dev-dependencies]
proptest = "1.0"
criterion = "0.5"

[[bench]]
name = "component_bench"
harness = false
