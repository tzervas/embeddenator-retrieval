name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  checks: write

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      - name: Clone dependency repos
        run: |
          cd ..
          # Clone path dependencies that this repo needs
          for repo in embeddenator-vsa embeddenator-fs; do
            if [ ! -d "$repo" ]; then
              git clone --depth 1 https://github.com/tzervas/$repo.git || true
            fi
          done

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          key: embeddenator-retrieval
      
      - name: Check formatting
        run: cargo fmt --check
      
      - name: Clippy lints
        run: cargo clippy --all-targets --all-features -- -D warnings
      
      - name: Build
        run: cargo build --all-features
      
      - name: Run tests
        run: cargo test --all-features --verbose

  # Optional: runs on self-hosted ARM64 runner if available
  build-arm64:
    name: Build (ARM64)
    runs-on: [self-hosted, linux, arm64]
    if: github.repository_owner == 'tzervas'
    continue-on-error: true
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          key: embeddenator-retrieval-arm64
      
      - name: Build
        run: cargo build --all-features
      
      - name: Run tests
        run: cargo test --all-features
